import React from 'react';
import UnderLineComponent from '../Utilites/UnderLineComponent';

const FourSteps = () => {
    return (
      <section className="fourSteps">
        <UnderLineComponent text="Wystarczą 4 proste kroki" />
      </section>
    );
};

export default FourSteps;